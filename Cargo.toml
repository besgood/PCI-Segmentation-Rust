[package]
name = "pci_segmentation_scanner_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4", features = ["derive"] }
chrono = "0.4"

calamine = "0.21"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
